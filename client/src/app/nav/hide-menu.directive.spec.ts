import { HideMenuDirective } from './hide-menu.directive';

describe('HideMenuDirective', () => {
  it('should create an instance', () => {
    const directive = new HideMenuDirective();
    expect(directive).toBeTruthy();
  });
});
