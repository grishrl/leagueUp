import { LStatDirective } from './l-stat.directive';

describe('LStatDirective', () => {
  it('should create an instance', () => {
    const directive = new LStatDirective();
    expect(directive).toBeTruthy();
  });
});
