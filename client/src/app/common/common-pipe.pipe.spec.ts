import { CommonPipePipe } from './common-pipe.pipe';

describe('CommonPipePipe', () => {
  it('create an instance', () => {
    const pipe = new CommonPipePipe();
    expect(pipe).toBeTruthy();
  });
});
