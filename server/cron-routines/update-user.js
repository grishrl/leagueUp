/*
this may no longer be needed as updating the team also updates the user MMR, stats parsing likewise handles the user stats
 */
const User = require('../models/user-models');

async function updateUsers() {

}

module.exports = {
    updateUsers: updateUsers
}